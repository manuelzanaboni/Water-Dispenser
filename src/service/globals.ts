export enum GPIO_MODE {
    IN = "in",
    OUT = "out",
}

export enum GPIO_STATE {
    LOW = "0",
    HIGH = "1",
}

export const RELAYS = {
    RELAY1: 2,
    RELAY2: 4,
    RELAY3: 5,
    RELAY4: 8,
}
